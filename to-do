--- IMPLEMENTATION ---

- cli error handling everywhere

- cli add 'new ' command to create and rename in one step

- cli delete workspace command

--- PRIORITY 2 ---

- cli consistent whitespace when printing
    - e.g. assignment has 0 lines, addition has 1 line

x cli enforce matrix names as single uppercase letters
    x autocorrect lowercase single letters to uppercase

- matrix str improve spacing for lopsided column sizes

- cli support set_cell with non int

- add deep fraction support
    - which levels of abstraction?
	- Matrix, Monomial, Polynomial + cli

- cli autosave / autoload
    - autoload functionality with .last workspace file
	    - populate save load exit with relevant func calls
	- consider flag implementation
	    - --no-autoload / -nal
	    - --no-autosave / -nas
	    - --new (same as nal?)

- cli help command

--- LOW PRIORITY ---

x cli ws should alphabetize filenames
x cli make listall and list alphabetical

--- COMPLETE ---

x fix print spacing for 1+ char cells

x cli swap file to prevent corrupted workspace save
x cli set cell value (implemented in workspace already)
x cli add newlines after print commands for legibility
x cli multiple commands in sequence (use | perhaps)
x cli load/(quit?) warn on potential data loss
x cli autosave / autoload -- base functionality
x cli clear named matrix
x cli >> save output to value
x cli list (list labels AND list label + value)
x cli list loadable files (ws)
x cli add feedback with saving, loading, etc
x cli save load

x add symbolic logic
    x independant polynomial engine in own file

--- DEBUG ---

- prevent untitled.json from being last workspace

- add validation to various user inputs
    - savename loadname setname
    - matrix variable name A B C

--- LATER --- 

- start gui
- determinant logic

--- TESTING --- 

x save/load in live cli

















